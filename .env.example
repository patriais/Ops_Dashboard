# Copia este archivo a .env y rellena con las credenciales reales (el .env está gitignoreado).
# default_isa_server.js necesita estas dos:
DB_RAILWAY=postgresql://USER:PASSWORD@HOST:PORT/railway
DB_HEROKU=postgres://USER:PASSWORD@HOST:PORT/DBNAME
